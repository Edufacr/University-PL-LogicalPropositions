# University-PL-LogicalPropositions
Fourth project of the Programming Languages course.

## How to compile

ghc --make -o bin/main src/main.hs -odir bin -hidir bin -isrc/

- El directorio bin debe existir para que funcione el comando